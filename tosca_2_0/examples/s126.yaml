tosca_definitions_version: tosca_2_0
# tag::s126[]
service_template:
  inputs:
    number-of-left:
      type: integer

  node_templates:
    right:
      type: Right

    left:
      type: Left
      count: { $get_input: number-of-left }
      requirements:
      - uses: right
# end::s126[]
