tosca_definitions_version: tosca_2_0
# tag::s57[]
service_template:
  node_templates:
    my-application:
      type: WebApplication
      requirements:
      - database: 
          node: my-database
          capability: Endpoint.Database
          relationship: CustomDbConnection
# end::s57[]
